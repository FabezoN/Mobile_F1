import { StyleSheet, Text, View, TouchableOpacity } from 'react-native';

export function Drivers() {
    return (
      <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
        <Text>Ceci est un pilote</Text>
      </View>
    );
  }
  